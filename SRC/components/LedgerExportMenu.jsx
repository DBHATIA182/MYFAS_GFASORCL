import ReportExportMenu from './ReportExportMenu';

/** @deprecated Use ReportExportMenu — kept for ledger desktop header. */
export default function LedgerExportMenu(props) {
  return <ReportExportMenu variant="dark" {...props} />;
}
